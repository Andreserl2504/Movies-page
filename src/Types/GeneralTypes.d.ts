export type genres = "Fantasy" | "Suspence"

export type imdbID = `tt${string}`

export type type = "Movie" | "Series" | "movies" | "series" | "game"

export type errorMessage = {
    errorMessage: string
}